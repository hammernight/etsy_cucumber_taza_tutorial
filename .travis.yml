before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start

script: "bundle exec rspec spec HEADLESS=true"
script: "bundle exec cucumber HEADLESS=true"

language: ruby
rvm:
  - 1.8.7